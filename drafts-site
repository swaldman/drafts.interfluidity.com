#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
if [[ "$SCRIPT_DIR" != "$(pwd -P)" ]]; then
    echo "Error: run this from its own directory ($SCRIPT_DIR), not $(pwd -P)" >&2
    exit 1
fi

./mill run "$@"


